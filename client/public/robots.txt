User-agent: *
Allow: /

# Private / administrative surfaces (also marked noindex in the app).
Disallow: /dashboard

# The website origin does not serve the API; keep it out of the crawl budget.
Disallow: /api/

Sitemap: https://falfoos.com/sitemap.xml
